import test from 'tape';
import {JSDOM} from 'jsdom';

import rootReducer from '../../src/root-reducer';

test('the root reducer should return current state if passed no valid action', assert => {

});